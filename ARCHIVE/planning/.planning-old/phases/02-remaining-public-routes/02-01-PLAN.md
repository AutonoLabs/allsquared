# 02-01 Plan

## Objective

Replace the remaining public-route pages with the shared AllSquared brand system, using `AllSquared.html` as the visual and tonal base instead of the older M3/Framer pages.

## Scope

- Rewrite the core marketing routes: `HowItWorks`, `Features`, `Pricing`, `LegalServices`, `About`, and `Contact`
- Rewrite the audience pages: `Freelancers` and `Clients`
- Keep `Terms` and `Privacy` at minimum visually consistent if they remain substantively unchanged
- Preserve the existing CTA/auth flow to `/dashboard`

## Tasks

- Add shared page-level marketing primitives on top of the existing homepage section system
- Move repeated public-route CTA logic into a shared helper
- Replace route-local gradients, M3 cards, and Framer-heavy structures with the live AllSquared paper/navy/green system
- Reuse existing extracted data where possible instead of re-authoring the same marketing content repeatedly
- Update Linear when a route batch is complete and identify any new follow-up tickets
