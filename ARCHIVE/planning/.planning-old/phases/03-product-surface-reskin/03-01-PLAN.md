# 03-01 Plan

## Objective

Bring the dashboard and admin shells into the AllSquared brand system while keeping operational pages dense, calm, and task-focused.

## Scope

- Reskin `DashboardLayout` and `AdminLayout` first
- Align product navigation, sidebars, loading states, and shell surfaces with the public paper/navy/green token system
- Avoid marketing-scale layouts inside product workflows
- Keep existing dashboard routes and behaviors unchanged

## Tasks

- Inspect current dashboard/admin shell structure and shared components
- Replace obvious M3/dark-primary shell styling with AllSquared operational tokens
- Keep nav hierarchy stable and readable for repeated use
- Verify with `pnpm check` and `pnpm build`
