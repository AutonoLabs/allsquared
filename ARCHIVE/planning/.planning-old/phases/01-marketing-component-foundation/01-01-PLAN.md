# 01-01 Plan

## Objective

Extract the current `Home.tsx` implementation into reusable marketing components and shared data while keeping the rendered result aligned to `AllSquared.html`.

## Tasks

- Move marketing content arrays and types into a shared content module
- Extract shared heading and CTA primitives
- Extract homepage sections into reusable marketing components
- Reduce `Home.tsx` to orchestration and auth/CTA wiring
- Verify the refactor with `git diff --check` and update Linear
