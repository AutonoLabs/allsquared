# 03-02 Plan

## Objective

Normalize the highest-visibility dashboard components so the product area feels consistent with the new shell while remaining operational and information-dense.

## Scope

- Focus first on dashboard home and contracts list surfaces
- Reduce remaining MD3 visual drift in page headers, stat cards, empty states, and status badges
- Keep behavioral changes out of this phase unless they are needed for correctness

## Tasks

- Inspect shared MD3 component usage on dashboard home and contracts pages
- Replace user-facing dashboard home components with standard UI/card/button primitives or AllSquared utility classes
- Align contract list cards and empty states with the operational palette
- Verify with `pnpm check` and `pnpm build`
